# Dataset

The training dataset is intentionally not included in this public repository.

Place the CSV locally at:

`data/combined_SMS_Spam_and_Ham_dataset.csv`

or set `DATA_PATH` in `.env`.

Do not upload datasets unless you have permission to redistribute them.
