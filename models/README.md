# Model Checkpoints

The trained `best_model.pt` checkpoint is intentionally excluded from this repository because it is large.

To reproduce the model checkpoint, run the training notebook. The default local output is:

`models/best_model.pt`

For a public model release, use an appropriate model-hosting service or Git LFS.
